spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: ${sql-server-password}
        url: jdbc:mysql//dev.donghyeon.dev
        username: root
sql-server-password: ${SQL_PASSWORD}
