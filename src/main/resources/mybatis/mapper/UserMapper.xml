<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.hoin.boardStudy.user.mapper.UserMapper">

    <resultMap id="userMap" type="user">
        <result column="user_id" property="userId"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="reg_date" property="regDate"/>
        <result column="withd_date" property="withdDate"/>
        <result column="upd_date" property="updDate"/>
        <result column="withd_status" property="withdStatus"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="gender" property="gender"/>
    </resultMap>

    <!-- 유저정보 체크 -->

    <select id="getUserInfo" resultMap="userMap">
        SELECT *
        FROM TBL_USER
        WHERE user_id = #{userId}
        AND withd_status = 0

    </select>


    <!-- 회원가입 -->
    <insert id="saveUser" parameterType="user">
        INSERT INTO TBL_USER
        (user_id, password, email, reg_date, withd_date, upd_date, withd_status, name, phone, gender)
        VALUES(#{userId}, #{password}, #{email}, sysdate(), null, sysdate(), 0, #{name}, #{phone}, #{gender});
    </insert>

    <!-- 회원정보수정 -->
    <update id="updateUserInfo" parameterType="user">
        UPDATE TBL_USER
        SET email = #{email},
                    #{phone},
                    updDate = sysdate()
        WHERE user_id = #{userId}
    </update>

    <!-- 이메일 중복 체크 -->
    <select id="emailCheck" parameterType="String" resultType="int">
        select count(email) from tbl_user where email=#{email}
    </select>

    <!-- 전화번호 중복 체크 -->
    <select id="phoneCheck" parameterType="String" resultType="int">
        select count(phone) from tbl_user where phone=#{phone}
    </select>

    <!-- 탈퇴 -->
    <!-- 로컬데이트타임으로 변경 예정-->
    <update id="withdrawUser">
        UPDATE TBL_USER
        SET withdStatus = 1, withdDate = sysdate()

        WHERE user_id = #{userId}
    </update>


</mapper>
