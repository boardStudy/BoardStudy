<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.hoin.boardStudy.user.mapper.UserMapper">

    <resultMap id="userMap" type="user">
        <result column="user_id" property="userId"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="reg_date" property="regDate"/>
        <result column="withd_date" property="withdDate"/>
        <result column="upd_date" property="updDate"/>
        <result column="withd_status" property="withdStatus"/>
<<<<<<< Updated upstream
        <result column="name" property="name"/>
=======
>>>>>>> Stashed changes

    </resultMap>

    <!-- 유저정보 체크 -->
<<<<<<< Updated upstream
    <select id="getUserInfo" resultMap="userMap">
=======
    <select id="userCheck" resultMap="userMap">
>>>>>>> Stashed changes
        SELECT *
        FROM TBL_USER
        WHERE user_id = #{userId}
        AND withd_status = 0

    </select>

<<<<<<< Updated upstream
    <!-- 회원가입 -->
    <insert id="saveUser" parameterType="user">
        INSERT INTO TBL_USER
        (user_id, password, email, reg_date, withd_date, upd_date, withd_status, name)
        VALUES(#{userId}, #{password}, #{email}, sysdate(), null, sysdate(), 0, #{name});
=======

    <!-- 회원가입 -->
    <insert id="saveUser" parameterType="user">
        INSERT INTO TBL_USER
        (user_id, password, email, reg_date, withd_date, upd_date, withd_status)
        VALUES(#{userId}, #{password}, #{email}, sysdate(), null, sysdate(), 0);
>>>>>>> Stashed changes
    </insert>

    <!-- 탈퇴 -->
    <!-- 로컬데이트타임으로 변경 예정-->
    <update id="withdrawUser">
        UPDATE TBL_USER
        SET withdStatus = 1, withdDate = sysdate()
<<<<<<< Updated upstream
        WHERE user_id = #{userId}
    </update>
=======
        WHERE user_id = #{userId}
    </update>

    <!-- 로그인
    <select id="login" resultMap="userMap">
        SELECT *
        FROM TBL_USER
        WHERE user_id = #{userId}
    </select>
    -->

>>>>>>> Stashed changes

</mapper>